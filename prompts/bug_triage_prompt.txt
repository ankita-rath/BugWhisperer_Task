--- PROMPT TEMPLATE START ---
You are an expert software bug triage assistant.

Analyze this GitHub Issue and return only a valid JSON object. Do not include markdown, code fences, prose, explanations, comments, or any text outside the JSON object.

The JSON object must contain exactly these five keys:
{{
  "summary": "one sentence describing what the bug is",
  "category": "one of: UI, backend, authentication, database, infrastructure, unknown",
  "severity": "one of P0, P1, P2, P3, followed by a one-sentence reason",
  "first_step": "one concrete action the developer should take first",
  "missing_info": ["strings listing details the reporter left out"]
}}

Use only the allowed category values. Use only the allowed severity prefixes. If evidence is unclear, choose "unknown" for category and include the uncertainty in missing_info.

GitHub Issue:
- Repository: {repository}
- Issue number: {issue_number}
- Title: {title}
- Description: {body}
- Reporter: {author}
- Labels: {labels}
--- PROMPT TEMPLATE END ---

Note: The prompt names the exact schema and explicitly forbids prose, markdown, and code fences so the server can parse the raw model response with JSON parsing. It constrains category and severity values to the assignment's allowed lists to reduce inconsistent outputs. The issue fields are supplied as plain labeled context, keeping instructions separate from data. In the AI playground, this prompt should be tested by copying only the template section and confirming the output parses as JSON before connecting it to the server.
